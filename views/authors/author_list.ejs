<h1><%= title %></h1>
<ul>
  <% if(author_list.length == 0){ %>
    <li>There are no authors.</li>
  <% }else{ %>
    <% author_list.forEach(function(author){ %>
      <li>
        <a href="<%= author.url %>"><%= author.name %></a>
        | Lifespan: <span style="color: green"><%= author.lifespan %></span>
      </li>
    <% }); %>
  <% } %>
</ul>
